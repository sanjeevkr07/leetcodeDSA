class Solution {
public:
    string removeOccurrences(string s, string port) {
        
        
        while(s.length()!=0 && s.find(port)<s.length()){
            s.erase(s.find(port),port.length());
        }
        return s;
    }
};
